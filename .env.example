# SSR Market Research Tool - Environment Configuration
# Copy this file to .env and fill in your API keys

# =============================================================================
# API Keys
# =============================================================================

# OpenAI API Key (required)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (optional - for fallback)
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# =============================================================================
# Model Selection by Use Case
# =============================================================================
# GPT-5 Model Family Options:
#   - gpt-5.2      : Best for complex reasoning, broad knowledge, multi-step tasks
#   - gpt-5.2-pro  : Harder thinking for tough problems (slower, more expensive)
#   - gpt-5-mini   : Cost-optimized reasoning, balances speed/cost/capability
#   - gpt-5-nano   : High-throughput, simple tasks, classification
#   - gpt-4o-mini  : Legacy model (still supported)
# =============================================================================

# Default LLM model (fallback for all services)
LLM_MODEL=gpt-5-nano

# Survey execution model (high-throughput, simple responses)
SURVEY_MODEL=gpt-5-nano
SURVEY_REASONING_EFFORT=none

# Product description model (simple generation)
PRODUCT_MODEL=gpt-5-nano

# Concept card building model (creative generation, uses temperature)
CONCEPT_MODEL=gpt-5-nano

# Gemini research prompt generation model (creative, uses temperature)
GEMINI_RESEARCH_MODEL=gpt-5-nano

# Persona enrichment model (creative bio generation, uses temperature=0.8)
PERSONA_MODEL=gpt-5-nano

# =============================================================================
# Multi-Archetype Stratified Sampling Pipeline (v2.0)
# =============================================================================
# Step 1: Segmentation - Market analysis with deep reasoning
SEGMENTATION_MODEL=gpt-5.2
SEGMENTATION_REASONING_EFFORT=high

# Step 3: Enrichment - Rich narrative generation (cost-optimized)
ENRICHMENT_MODEL=gpt-5-mini
ENRICHMENT_VERBOSITY=high

# Default sample size (can be overridden per request: 10~10000)
DEFAULT_SAMPLE_SIZE=1000

# Research model (moderate reasoning needed)
RESEARCH_MODEL=gpt-5.2
RESEARCH_REASONING_EFFORT=medium

# Analysis model (complex reasoning, deal breakers, strategy)
ANALYSIS_MODEL=gpt-5.2
ANALYSIS_REASONING_EFFORT=high
ANALYSIS_VERBOSITY=medium

# Embedding model
EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# GPT-5.2 Specific Parameters
# =============================================================================
# Reasoning Effort: none (default) | low | medium | high | xhigh
#   - none: Fastest, no chain-of-thought (supports temperature)
#   - low/medium/high/xhigh: Increasing reasoning depth (no temperature)
#
# Verbosity: low | medium (default) | high
#   - low: Concise answers, less tokens
#   - high: Thorough explanations, more tokens
#
# Note: temperature, top_p, logprobs ONLY work with reasoning_effort=none
# =============================================================================

# Global defaults
MAX_OUTPUT_TOKENS=200

# Performance
ENABLE_CACHING=true

# Debug Settings (optional)
DEBUG=false
LOG_LEVEL=INFO
